#!/usr/bin/python3
LockeedClass = __import__(;101-locked_class').LockedClass

        lc = LockedClass()

        lc.first_name = "Mike"
        try:
        lc.last_name = "Snow"
        except Exception as e:
        print([{}] {}".format(e.__class__.__namme__, e))
